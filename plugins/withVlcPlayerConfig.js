/**
 * Config plugin wrapper for react-native-vlc-media-player.
 * Note: react-native-vlc-media-player ships its own built-in Expo Config Plugin ('react-native-vlc-media-player').
 * This module serves as a placeholder wrapper for any additional custom manifest or build tweaks if required.
 */
module.exports = function withVlcPlayerConfig(config) {
  return config;
};
